import { SwalProvider } from './SwalProvider';
import { useSwalDialog } from './composable';
import type { ISwalDialogApi } from './SwalProvider';

export { SwalProvider, useSwalDialog };
export type { ISwalDialogApi };
